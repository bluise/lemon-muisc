# 柠檬音乐 v1.2.11

## 更新

### 修复

- **手机搜索 / 发现列表排版**：父页桌面 grid 误覆盖曲目行，导致歌名被挤成竖条换行；现恢复为勾选 + 歌名/歌手 + 试听 / 加入 / 下载

本版仍为原生独立应用（不依赖 Docker），依赖应用中心 Node.js v22。

## 安装包

- `lemon-music-1.2.11-x86.fpk`
- `lemon-music-1.2.11-arm.fpk`
