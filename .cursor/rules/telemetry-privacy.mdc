---
description: 日活上报隐私 — 仓库不落真实 TELEMETRY 地址/密钥；仅本地 FPK 打包注入
alwaysApply: true
---

# 日活上报（Telemetry）隐私规则

## 硬性要求（每次提交 / 每次改代码都遵守）

1. **禁止**把真实上报 URL、密钥、域名写进会被 git 跟踪的文件（含 `README`、注释、示例里的真域名、PHP 示例里的真 secret）。
2. **允许**的配置来源只有：
   - 环境变量 `TELEMETRY_URL`、`TELEMETRY_SECRET`（优先）
   - 本地 `server/telemetry.local.json` / `config/telemetry.local.json`（已 gitignore）
   - 可选 `urlB64` / `secretB64`（仍只放在 gitignore 的本地文件里）
3. 仓库里只保留占位示例：`server/telemetry.local.json.example`、`tools/lemon-telemetry-php/config.example.php`。
4. **不要**在代码里硬编码接收端域名；`server/utils/telemetry.js` 只从 env / 本地文件读取。

## FPK / Releases

- **git 提交**：不含真实上报配置。
- **本地 `npm run fpk:build`**：从本地 `telemetry.local.json` 或 `TELEMETRY_*` 注入进安装包，再上传 Releases。
- 未配置上报时默认 **打包失败**（可用 `-AllowNoTelemetry` 仅作无上报测试包）。

## 提交前自检

- `git status` / diff 中不得出现真实 `TELEMETRY_SECRET`、自有域名、`telemetry.local.json` 实文件。
- 不要 `git add -f` 强制加入 gitignore 的 telemetry / `config.php`。
